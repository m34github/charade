const common = {
  article: {
    height: window.innerHeight
  }
};

const home = {
  image: {
    paddingTop: 12,
    paddingBottom: 12
  }
};

const upload = {
  button: {
    width: 320,
    textAlign: 'left'
  },
  input: {
    display: 'none'
  }
};

const style = { common, home, upload };

export default style;
